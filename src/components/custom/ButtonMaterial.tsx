import { Button } from "@material-tailwind/react";

export function ButtonDefault() {
  return (
    <Button variant="solid" color="secondary" className="uppercase">
      Button
    </Button>
  );
}

export function ButtonPrimary() {
  return (
    <Button variant="solid" color="primary" className="uppercase">
      Button
    </Button>
  );
}
